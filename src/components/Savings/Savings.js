import React from 'react';

export const Savings = ({ all }) => {

    /*
        if calculate display loan calculations
        else show inputs
    */
    return (  
        <div className="">
            savings calculator
        </div>
    );
};


export default Savings;
