import React from 'react';

class EndAmount extends React.Component {

    render() {
        //const username = this.state.username;
        //const userStatus = this.props.logged;
        //console.log('--- render login ---');
        return (
            <div>
                Final amount:
            </div>
        );
    }
};

export default EndAmount;
